rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /diaries/{diaryId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /favorites/{favoriteId} {
        allow read, delete: if request.auth != null
                            && request.auth.uid == userId;
        allow create: if request.auth != null
                      && request.auth.uid == userId
                      && request.resource.data.keys().hasOnly(['sharedDiaryId', 'createdAt'])
                      && request.resource.data.sharedDiaryId is string
                      && request.resource.data.sharedDiaryId == favoriteId
                      && request.resource.data.createdAt is timestamp
                      && request.resource.data.createdAt == request.time;
      }

      match /settings/appearance {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /settings/profile {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /settings/memory/{memoryPath=**} {
        allow read, write: if request.auth != null
                           && request.auth.uid == userId;
      }
    }

    match /sharedDiaries/{shareId} {
      allow read: if true;
      allow create: if request.auth != null
                    && request.resource.data.uid == request.auth.uid;
      allow update, delete: if request.auth != null
                            && resource.data.uid == request.auth.uid;
    }
  }
}
